import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <h1>Hello from Aakriti 🎉</h1>
    <p>This app supports push notifications and can be installed as a PWA.</p>
  </React.StrictMode>
);